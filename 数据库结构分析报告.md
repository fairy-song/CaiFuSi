# 财赋思项目数据库结构分析报告

## 📊 数据库概览

### 数据库类型
- **主数据库**: Firebase Firestore (NoSQL云数据库)
- **开发模式**: 内存存储 (Python字典模拟)
- **前端认证**: Firebase Authentication

---

## 🏗️ 数据库架构

### 1. 整体结构

```
artifacts/
└── {app_id}/
    ├── users/
    │   └── {user_id}/
    │       ├── profile/              # 用户资料
    │       ├── assessments/          # 财务评估结果
    │       ├── coach_messages/       # AI教练对话记录
    │       ├── goals/                # 财务目标
    │       └── transactions/         # 交易记录
    └── public/
        └── data/
            └── knowledge_base/       # 公共知识库
```

---

## 📁 数据集合详解

### 1.1 用户资料 (profile)

**路径**: `artifacts/{app_id}/users/{user_id}/profile/{user_id}`

**数据结构**:
```json
{
  "email": "user@example.com",
  "displayName": "用户昵称",
  "createdAt": "2026-03-07T09:00:00Z",
  "lastLogin": "2026-03-07T09:00:00Z",
  "assessmentCompleted": false,
  "goals": []
}
```

**字段说明**:
| 字段 | 类型 | 说明 |
|------|------|------|
| email | String | 用户邮箱 |
| displayName | String | 显示名称 |
| createdAt | Timestamp | 创建时间 |
| lastLogin | Timestamp | 最后登录时间 |
| assessmentCompleted | Boolean | 是否完成评估 |
| goals | Array | 用户目标列表 |

---

### 1.2 财务评估 (assessments)

**路径**: `artifacts/{app_id}/users/{user_id}/assessments/latest`

**数据结构**:
```json
{
  "total_score": 3.2,
  "scores": {
    "savings": 3.5,
    "risk": 2.8,
    "emergency": 3.0,
    "debt": 3.8,
    "knowledge": 2.5,
    "income": 3.2,
    "goals": 3.0,
    "spending": 2.9
  },
  "completed": true,
  "completedAt": "2026-03-07T09:00:00Z",
  "recommendations": [
    {
      "priority": "high",
      "category": "emergency",
      "message": "建议建立应急基金"
    }
  ]
}
```

**评分维度**:
1. **savings** (储蓄能力): 0-4分
2. **risk** (风险管理): 0-4分
3. **emergency** (应急准备): 0-4分
4. **debt** (债务管理): 0-4分
5. **knowledge** (金融知识): 0-4分
6. **income** (收入稳定性): 0-4分
7. **goals** (目标规划): 0-4分
8. **spending** (支出控制): 0-4分

**风险等级划分**:
- **3.5-4.0**: 进取型 (高风险承受能力)
- **2.5-3.5**: 平衡型 (中等风险承受能力)
- **1.5-2.5**: 保守型 (较低风险承受能力)
- **0-1.5**: 极度保守型 (很低风险承受能力)

---

### 1.3 AI教练对话 (coach_messages)

**路径**: `artifacts/{app_id}/users/{user_id}/coach_messages/{message_id}`

**数据结构**:
```json
{
  "id": "msg_uuid",
  "sender": "user",
  "text": "我想了解如何理财",
  "timestamp": "2026-03-07T09:00:00Z"
}
```

**字段说明**:
| 字段 | 类型 | 可选值 | 说明 |
|------|------|--------|------|
| id | String | - | 消息唯一标识 |
| sender | String | "user" / "ai" | 发送者类型 |
| text | String | - | 消息内容 |
| timestamp | Timestamp | - | 发送时间 |

**查询限制**:
- 默认获取最近20条消息
- 按时间倒序排列
- 需要在Firestore中创建timestamp索引

---

### 1.4 财务目标 (goals)

**路径**: `artifacts/{app_id}/users/{user_id}/goals/{goal_id}`

**数据结构**:
```json
{
  "id": "goal_uuid",
  "title": "购房首付",
  "target_amount": 500000,
  "current_amount": 150000,
  "deadline": "2027-12-31",
  "status": "active",
  "progress": 30,
  "timestamp": "2026-03-07T09:00:00Z",
  "updated_at": "2026-03-07T09:00:00Z"
}
```

**字段说明**:
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 目标唯一标识 |
| title | String | 目标标题 |
| target_amount | Number | 目标金额 |
| current_amount | Number | 当前金额 |
| deadline | String | 截止日期 |
| status | String | 状态: active/completed/cancelled |
| progress | Number | 进度百分比 (0-100) |
| timestamp | Timestamp | 创建时间 |
| updated_at | Timestamp | 更新时间 |

---

### 1.5 交易记录 (transactions)

**路径**: `artifacts/{app_id}/users/{user_id}/transactions/{transaction_id}`

**数据结构**:
```json
{
  "id": "trans_uuid",
  "type": "expense",
  "amount": 1500,
  "category": "餐饮",
  "description": "周末聚餐",
  "timestamp": "2026-03-07T09:00:00Z"
}
```

---

### 1.6 公共知识库 (knowledge_base)

**路径**: `artifacts/{app_id}/public/data/knowledge_base/{article_id}`

**数据结构**:
```json
{
  "id": "article_uuid",
  "title": "理解ETF投资",
  "content": "ETF是交易型开放式指数基金...",
  "category": "投资理财",
  "tags": ["etf", "股票", "基金"],
  "createdAt": "2026-03-07T09:00:00Z"
}
```

---

## 🔧 开发模式数据存储

### 内存数据库结构

```python
_dev_db = {
    "users": {
        "user_id_1": {
            "profile": {...},
            "coach_messages": [...],
            "assessments": {
                "latest": {...}
            },
            "goals": [...],
            "transactions": [...]
        }
    },
    "public": {
        "knowledge_base": [...]
    }
}
```

**特点**:
- 数据存储在Python进程内存中
- 服务重启后数据丢失
- 适用于开发和测试环境
- 无需配置Firebase

---

## 🔐 数据库配置

### Firebase配置 (前端)

**文件**: [`src/firebase.js`](src/firebase.js:1)

**环境变量**:
```env
REACT_APP_FIREBASE_API_KEY=your_api_key
REACT_APP_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your-project-id
REACT_APP_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
REACT_APP_FIREBASE_APP_ID=your_app_id
REACT_APP_FIREBASE_MEASUREMENT_ID=your_measurement_id
```

### Firebase Admin SDK (后端)

**文件**: [`backend/app/config.py`](backend/app/config.py:1)

**环境变量**:
```env
FIREBASE_ADMIN_SDK_PATH=path/to/serviceAccountKey.json
```

---

## 📊 数据服务层

### 核心服务模块

#### 1. Firestore服务 ([`firestore_service.py`](backend/app/services/firestore_service.py:1))

**主要功能**:
- 用户资料管理 (CRUD)
- 评估结果存储
- 对话历史记录
- 知识库管理

**关键函数**:
```python
# 用户管理
create_user_profile(user_id, email, display_name)
get_user_profile(user_id)
update_user_profile(user_id, data_to_update)

# 评估数据
save_assessment_results(user_id, assessment_data)
get_assessment_results(user_id)

# 对话记录
save_coach_message(user_id, message_data)
get_coach_history(user_id, limit=20)

# 知识库
add_kb_article(article_data)
get_kb_articles(category=None, limit=10)
```

#### 2. 用户数据服务 ([`user_data_service.py`](backend/app/services/user_data_service.py:1))

**主要功能**:
- 通用数据存储接口
- 数据查询和过滤
- 统计数据生成

**关键方法**:
```python
save_user_data(user_id, data_type, data)
get_user_data(user_id, data_type, limit=10)
get_latest_data(user_id, data_type)
update_user_data(user_id, data_type, data_id, updates)
delete_user_data(user_id, data_type, data_id)
get_user_statistics(user_id)
```

#### 3. 用户画像服务 ([`user_profile_service.py`](backend/app/services/user_profile_service.py:1))

**主要功能**:
- 构建用户财务画像
- 生成AI上下文信息
- 财务健康度评估

**关键方法**:
```python
build_user_profile(user_id)
get_user_context_for_ai(user_id)
update_user_goal(user_id, goal_data)
get_user_goals(user_id, status=None)
```

---

## 📈 用户画像系统

### 财务健康度评估

**等级划分**:
| 总分范围 | 健康等级 | 说明 |
|---------|---------|------|
| 3.5-4.0 | 优秀 | 财务状况非常健康 |
| 2.5-3.5 | 良好 | 财务状况较好 |
| 1.5-2.5 | 一般 | 财务状况需要改进 |
| 0-1.5 | 需改进 | 财务状况较差 |

### AI上下文生成

**包含信息**:
1. 财务评估总分和各维度得分
2. 风险偏好和承受能力
3. 当前财务目标
4. 高优先级建议
5. 评估历史统计

**示例输出**:
```
用户财务评估总分: 3.2/4.0
各维度得分:
  - 储蓄能力: 3.5/4.0
  - 风险管理: 2.8/4.0
  - 应急准备: 3.0/4.0

风险偏好: 平衡型
风险承受能力: 中等

当前财务目标数量: 3
活跃目标:
  - 购房首付
  - 子女教育基金
  - 退休储蓄

高优先级建议数量: 2
评估完成次数: 5
最后评估时间: 2026-03-07T09:00:00Z
```

---

## 🔍 数据查询优化

### Firestore索引需求

**必需索引**:
1. **coach_messages**: timestamp (降序)
2. **knowledge_base**: category + createdAt (降序)
3. **goals**: status + timestamp (降序)
4. **transactions**: timestamp (降序)

**创建方式**:
- 在Firebase控制台手动创建
- 或通过firestore.indexes.json配置文件

---

## 🚀 性能考虑

### 数据限制

1. **Firestore文档大小**: 最大1MB
2. **对话历史**: 建议使用子集合而非数组
3. **查询限制**: 默认limit=20，避免大量数据传输

### 优化建议

1. **分页查询**: 使用startAfter()实现分页
2. **缓存策略**: 前端缓存常用数据
3. **批量操作**: 使用batch写入减少请求次数
4. **索引优化**: 为常用查询创建复合索引

---

## 🔒 安全规则

### Firestore安全规则示例

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 用户只能访问自己的数据
    match /artifacts/{appId}/users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // 公共知识库所有人可读
    match /artifacts/{appId}/public/data/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

---

## 📝 数据迁移

### 从开发模式到生产模式

**步骤**:
1. 配置Firebase项目
2. 设置环境变量
3. 初始化Firebase Admin SDK
4. 将DEV_MODE设置为false
5. 测试数据读写功能

**注意事项**:
- 开发模式数据不会自动迁移
- 需要手动导出/导入测试数据
- 确保Firestore安全规则已配置

---

## 🛠️ 开发工具

### 数据库管理

1. **Firebase控制台**: https://console.firebase.google.com/
2. **Firestore模拟器**: 本地开发测试
3. **数据导入/导出**: 使用Firebase CLI

### 调试工具

```python
# 检查是否为开发模式
from backend.app.services.firestore_service import is_dev_mode
print(f"开发模式: {is_dev_mode()}")

# 查看内存数据库
from backend.app.services.firestore_service import _dev_db
print(f"当前用户数: {len(_dev_db['users'])}")
```

---

## 📚 相关文档

- [Firebase Firestore文档](https://firebase.google.com/docs/firestore)
- [Firebase Authentication文档](https://firebase.google.com/docs/auth)
- [项目配置指南](.env.example)
- [使用说明](使用说明.md)

---

## 🔄 更新日志

- **2026-03-07**: 初始版本，完整数据库结构分析
- 包含所有核心数据集合和服务层说明
- 添加开发模式和生产模式对比

---

**生成时间**: 2026-03-07  
**项目**: 财赋思 (Cái Fù Sī) - AI金融心智教练  
**数据库**: Firebase Firestore + 内存存储
