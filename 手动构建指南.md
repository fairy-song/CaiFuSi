# 手动构建前端项目指南

## 如果自动脚本失败,请按以下步骤手动构建

### 步骤1: 打开命令行

1. 按 `Win + R` 打开运行对话框
2. 输入 `cmd` 并按回车
3. 或者在项目文件夹中,按住 Shift 键,右键点击空白处,选择"在此处打开命令窗口"

### 步骤2: 进入项目目录

```bash
cd /d D:\BaiduNetdiskDownload\CaiFuSi
```

(请根据你的实际路径修改)

### 步骤3: 查找Node.js路径

运行以下命令查找Node.js:

```bash
where node
```

如果显示路径,说明Node.js在PATH中,可以直接使用。

如果提示找不到,尝试这些常见路径:

```bash
# 方法1
"C:\Program Files\nodejs\npm.cmd" run build

# 方法2
"C:\Program Files (x86)\nodejs\npm.cmd" run build

# 方法3
"%LOCALAPPDATA%\Programs\nodejs\npm.cmd" run build
```

### 步骤4: 构建项目

如果Node.js在PATH中:
```bash
npm run build
```

如果需要使用完整路径(根据步骤3的结果):
```bash
"C:\Program Files\nodejs\npm.cmd" run build
```

### 步骤5: 等待完成

构建过程需要2-5分钟,会显示类似这样的输出:
```
Creating an optimized production build...
Compiled successfully.
```

### 步骤6: 验证构建

检查 `build` 文件夹是否有新文件:
```bash
dir build
```

应该看到最新的日期时间。

### 步骤7: 重新启动

双击运行 `快速启动.cmd`

## 常见错误解决

### 错误1: 'npm' 不是内部或外部命令

**原因**: Node.js不在系统PATH中

**解决**: 
1. 找到Node.js安装目录(通常在 `C:\Program Files\nodejs`)
2. 使用完整路径运行:
   ```bash
   "C:\Program Files\nodejs\npm.cmd" run build
   ```

### 错误2: 找不到模块

**解决**: 先安装依赖
```bash
npm install
```
然后再构建:
```bash
npm run build
```

### 错误3: 权限错误

**解决**: 以管理员身份运行命令行
1. 搜索 `cmd`
2. 右键选择"以管理员身份运行"
3. 重新执行构建命令

## 最简单的方法

如果以上都不行,使用开发模式:

### 终端1 - 启动后端
```bash
cd backend
python run_dev_enhanced.py
```

### 终端2 - 启动前端开发服务器
```bash
npm start
```

开发模式会自动编译并监听代码更改,不需要手动构建。

访问 http://localhost:3000 即可看到最新的更改。

## 需要帮助?

如果还是无法构建,请提供:
1. 运行 `where node` 的输出
2. 运行 `node -v` 的输出
3. 运行 `npm -v` 的输出
4. 构建时的完整错误信息
